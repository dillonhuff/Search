package search

import org.junit.Assert._
import org.junit.Test

class DocumentCollectionTests {
	
	@Test
	def nonDocsHaveWords() = {
		val docColl = new DocumentCollection(Vector.empty[Document])
		
	}
}